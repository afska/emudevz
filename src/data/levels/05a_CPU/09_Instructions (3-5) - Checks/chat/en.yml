---
main:
  messages:
  - there are only a few checks
  - they are, basically, instructions that compare values with a register or perform logical operations
  - I don't think you will need help with these
  - 📚  _-_implement all the check instructions in your `instructions` object!_--_
  run-after-messages: |
    store.dispatch.savedata.openFile("/docs/cpu/instructions.en.md");
  responses: []
