---
main:
  messages:
  - como viste en el capítulo de Ensamblador, también existe un registro 🏁  <Flags> que representa el estado del procesador
  - |-
    es un registro de 8 bits, en el cual cada bit tiene un significado especial
     ```raw _______________________________```
    ```raw | 7 | 6 | 5 | 4 | 3 | 2 | 1 | 0 |```
    ```raw |---|---|---|---|---|---|---|---|```
    ```raw |``` ~N~ ```raw |``` ~V~ ```raw |``` ```raw 1``` ```raw |``` ```raw b``` ```raw |``` ~D~ ```raw |``` ~I~ ```raw |``` ~Z~ ```raw |``` ~C~ ```raw |```
     ```raw ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾```
  - ya cubrimos C ("Carry") y Z ("Zero") anteriormente, y hablaremos de los demás cuando empecemos a programar instrucciones de CPU
  - todas las banderas se representan con una letra en mayúscula
  responses:
  - no <todas> 😑 [notall]
  - <<level.isCompleted || m.$exercise>> 📚  llévame a la acción [what]

notall:
  messages:
  - bueno, bueno, con algunas excepciones
  - el bit 5 es siempre 1
  - el bit 4 ("b")... depende... de... cosas - vamos a ignorarlo por ahora 😂
  responses:
  - está bien, ¿qué debería hacer? [what]

what:
  run: |
    set((m) => m.$exercise = true);
  messages:
  - 📚  _-_crea una clase `FlagsRegister` con 6 propiedades booleanas (llamadas ~c~, ~z~, ~i~, ~d~, ~v~ y ~n~)_--_
  - _-_inicialízalas en ~false~_--_
  - |-
    📚  _-_luego implementa, siguiendo la tabla anterior:_--_
    *_-_*getValue()**:_--_
      *_-_*->** tiene que retornar un número de 8 bits con todas las banderas_--_
    *_-_*setValue(value)**:_--_
      *_-_*->** tiene que asignar todas las banderas a partir del número de 8 bits que recibe_--_
  - _-_p. ej. con ~i~ y ~v~ encendidas, ```javascript getValue()``` debería retornar ```javascript 0b01100100```_--_
  - _-_y ```javascript setValue(0b00000010)``` debería poner todos los booleanos en ~false~, menos ~z~_--_
  responses:
  - (*) necesito un poco de ayuda 📐 [bits]
  - ✅  ya tengo la clase [nowwhat]

bits:
  messages:
  - 📄  ~/lib/byte.js~ al rescate 🙌
  - para construir el valor en ```javascript getValue()```, usa `bitfield`
  - para obtener las banderas en ```javascript setValue(value)```, usa `getFlag`
  responses:
  - ...what

nowwhat:
  messages:
  - "📚  _-_perfecto, solo agrega una nueva instancia a la clase `CPU`, debajo de los otros registros, así:\n  ```javascript this.flags = new FlagsRegister();```_--_"
  responses: []
