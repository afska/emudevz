---
main:
  messages:
  - we'll define some helpers to simplify future tasks
  - |-
    📚  _-_in your `Register8Bit` and `Register16Bit` classes, implement:_--_
    **increment()**:
      __-> calls *setValue* with the result of getValue() + 1__
    **decrement()**:
      __-> calls *setValue* with the result of getValue() - 1__
  - |-
    _-_in your `FlagsRegister`, implement:_--_
    **updateZero(value)**:
      __-> sets z=true if value is 0, or z=false otherwise__
    **updateNegative(value)**:
      __-> sets n=true if byte.isNegative(value), or n=false otherwise__
    **updateZeroAndNegative(value)**:
      __-> calls *updateZero* and *updateNegative* with the <value>__
  responses: []
