---
main:
  messages:
  - we'll define some helpers to simplify future tasks
  - |-
    📚  _-_in your `Register8Bit` and `Register16Bit` classes, implement:_--_
    **increment()**:
      **->** calls ~setValue(...)~ with the result of ~getValue()+1~
    **decrement()**:
      **->** calls ~setValue(...)~ with the result of ~getValue()-1~
  - |-
    _-_in your `FlagsRegister`, implement:_--_
    **updateZero(value)**:
      **->** sets ~z=true~ if ~value~ is 0, or ~z=false~ otherwise
    **updateNegative(value)**:
      **->** sets ~n=true~ if ~byte.isNegative(value)~, or ~n=false~ otherwise
    **updateZeroAndNegative(value)**:
      **->** calls ~updateZero(...)~ and ~updateNegative(...)~ with the ~value~
  responses: []
