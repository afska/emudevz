---
main:
  messages:
  - "coming soon! // TODO: DISABLE INPUT, even gamepad"
  - "// TODO: first click both starts the music and pauses - race"
  - leaving should restart the music (preexisting bug; NavBar misses the resume-music event since the Loading screen unmounts the navbar)
  - prevent emulator from being launched, make it a level flag instead of a hack listener in triple layout
  responses:
  - try rom [start]

start:
  run: |-
    {{Background}}.loadROM("/roms/.all/SuperTiltBro.neees", "demoRom");
  messages:
  - does this work?
  responses: []
