---
main:
  messages:
  - "coming soon! // TODO: DISABLE INPUT, even gamepad"
  - prevent emulator from being launched, make it a level flag instead of a hack listener in triple layout
  responses:
  - try rom [start]

start:
  run: |-
    {{Background}}.loadROM("/roms/.all/SuperTiltBro.neees", "demoRom");
  messages:
  - does this work?
  responses: []
