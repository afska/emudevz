---
main:
  messages:
  - before going forward, let's enjoy what we build so far 😎
  - on the right side you have another terminal
  - |
    run the `repl` command and test the following code:

    ```javascript
    const newRom = (prgBytes) => {
      const header = [
        0x4e, 0x45, 0x53, 0x1a, // magic constant
        1, //                      PRG-ROM size
        0, //                      CHR-ROM size
        0b00000000, //             flags 6
        0b00000000, //             flags 7
        0, 0, 0, 0, 0, 0, 0, 0 //  padding
      ];
      return new Uint8Array([...header, ...prgBytes]);
    }

    $ // our code
    const testRom = newRom([0xe8, 0xc8, 0xe8, 0x8a]); // INX, INY, INX, TXA
    const neees = new $.NEEES(testRom); // create NEEES
    neees.step(); // run one instruction
    neees.cpu.x.getValue(); // inspect [X]
    neees.cpu.pc.getValue().toString(16); // inspect [PC] in hexadecimal```
  - 📚  when you're done, run the `test` command to continue
  events:
  - null [end]
