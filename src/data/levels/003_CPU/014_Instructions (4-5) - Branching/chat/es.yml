---
main:
  messages:
  - las bifurcaciones implican cambiar el valor de `[PC]` (el "Program Counter")
  - y eso es lo que la mayoría de estas instrucciones hacen
  - 📚  ¡implementa todas las instrucciones de bifurcaciones en tu objeto `instructions`!
  run-after-messages: |
    store.dispatch.savedata.openFile("/docs/cpu_instructions.es.md");
  responses: []
