---
main:
  messages:
  - data instructions are pretty simple
  - as the name suggests, they're all about storing and moving data 😉
  - 📚  implement all the data instructions in your `instructions` object!
  run-after-messages: |
    store.dispatch.savedata.openFile("/docs/cpu_instructions.en.md");
  responses: []
