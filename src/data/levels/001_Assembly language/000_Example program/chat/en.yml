---
main:
  run: |
    level.setMemory((memory) => memory.$canRun = false)
  messages:
  - asdasd
  responses:
    - test [aver]

aver:
  run: |
    level.setMemory((memory) => memory.$canRun = true)
  on_play: |
    goTo("lda")
  messages:
  - see? re piola
  responses:
    - piola [end]
