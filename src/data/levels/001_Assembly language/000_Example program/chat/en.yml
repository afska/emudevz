---
main:
  run: |
    // bus.emit("run-enabled", false)
  on-play: |
    goTo("lda")
  messages:
  - well, now press the play button...
  responses: []

lda:
  messages:
  - see? re piola
  responses: []
