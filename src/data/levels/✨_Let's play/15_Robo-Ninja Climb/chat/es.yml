---
main:
  run: |
    set((m) => m.chat.winOnEnd = true);
  messages:
  - hey,
  - juguemos un juego
  - |-
    se llama 🤖 🧗  ~Robo-Ninja Climb~
    <{screenshot.png;30x30}>
  - si lo completas, te enviaré todos mis 🕹️  juegos de <arcade>
  - en formato ~*.neees~
  responses:
  - suena fácil 🙌 [play]

play:
  run: |
    {{Right}}.stream.zoom();
    {{Right}}.stream.setIntegration("RoboNinjaClimb");
    {{Right}}.load("/roms/.all/RoboNinjaClimb.neees", "stream");
  messages:
  - bueno, no lo es 🙃
  - de hecho, morirás muchísimo
  - te esperaré aquí 👀
  events:
  - roboninjaclimb-end [finish]

finish:
  run: |
    const directory = `${Drive.ROMS_DIR}/arcade`;
    filesystem.mkdirp(directory);

    ["Heist.neees", "RoboNinjaClimb.neees", "Thwaite.neees"].forEach((file) => {
      filesystem.cp(`${Drive.ALLROMS_DIR}/${file}`, `${directory}/${file}`);
    });
  messages:
  - ¡WOW!
  - ¡qué juego difícil!
  - como prometí, puse mis ROMs en tu directorio 📁  ~/roms~
  - bien hecho 👏👏
  responses:
  - ▶️  continuar [end]
